PORT=3000
JWT_SECRET=dev_secret_change_me
APP_BASE_URL=http://app.madi-app.ru

# SMTP (optional; if empty, emails are logged to files in server/data/outbox)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="MADI App <no-reply@madi.local>"
